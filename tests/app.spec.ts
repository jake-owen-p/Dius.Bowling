import {BowlingGame} from "../src/app";

describe('GIVEN ', () => {
    describe('WHEN ', () => {
        it('THEN ', () => {
            const bowlingGame = new BowlingGame();
            expect(bowlingGame.score()).toEqual(0);
        })
    })
})
